[settings]
functionname = hi_vpri_set;
attributenum = 2;
attributeflag= 1;

[parameter]
attribute=alias<ifname>   type<string>  range<0,32>        default<nas0>;
attribute=alias<pri>      type<uint>    range<0,7>         default<0>;

[help]

#define HI_WANCMD_MAX_DEV_NAME_LEN 32

typedef struct
{
    hi_uchar8 uc_devname[HI_WANCMD_MAX_DEV_NAME_LEN]; 
    hi_uint32 ui_pri; 
}hi_vpri_info_s; 