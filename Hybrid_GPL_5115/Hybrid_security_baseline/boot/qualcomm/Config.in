config QUALCOMM_BOOT
    bool "support qualcomm boot"
	
if QUALCOMM_BOOT
config BOOTLOADER_VENDOR
	string
	default "QUALCOMM"

choice
	prompt "qualcomm boot edition"
	
config QUALCOMM_BOOT_1_000
	bool "qualcomm boot 1.000"

endchoice

endif


