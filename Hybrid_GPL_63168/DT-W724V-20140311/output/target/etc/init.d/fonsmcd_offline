#!/bin/sh

PATH=/sbin:/bin:/usr/sbin:/usr/bin

if [ -f /etc/default/chilli ] ; then
   . /etc/default/chilli
fi

/etc/init.d/chilli stop
foncontrl wifidown
echo "[OFFLINE]FON WIFI DOWN"
echo -n "Active" > $STATUSFILE

exit 0